# --- Restricted resources -------------------------------------------------
# Deep links to Drive folders shared with specific people only.
# NEVER commit anything from here. See the restricted-links rule in CLAUDE.md.
private/

# --- Build output ---------------------------------------------------------
# Generated by scripts/build.py and mkdocs. README.md, docs/mindmap.html and
# docs/concepts/ ARE committed (they are the published artifacts); these are not.
dist/
site/
.cache/

# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# --- Node (only if a JS tool is added later) ------------------------------
node_modules/
npm-debug.log*

# --- Claude Code ----------------------------------------------------------
# settings.json IS committed (shared hooks + permissions).
# Personal overrides are not.
.claude/settings.local.json

# --- Editors / OS ---------------------------------------------------------
.vscode/
.idea/
*.swp
Thumbs.db
Desktop.ini
ehthumbs.db
$RECYCLE.BIN/
.DS_Store
