# --- Restricted resources -------------------------------------------------
# Deep links to Drive folders shared with specific people only.
# NEVER commit anything from here. See the restricted-links rule in CLAUDE.md.
private/

# --- Build output ---------------------------------------------------------
# Generated, but COMMITTED because they are the published artifacts and CI's
# `build.py --check` compares them against a fresh build:
#   README.md, dist/webtech.ttl, docs/mindmap.html, docs/concepts/
# Not committed -- purely local rendering of the above:
site/
.cache/

# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# --- Node (only if a JS tool is added later) ------------------------------
node_modules/
npm-debug.log*

# --- Claude Code ----------------------------------------------------------
# settings.json IS committed (shared hooks + permissions).
# Personal overrides are not.
.claude/settings.local.json

# --- Editors / OS ---------------------------------------------------------
.vscode/
.idea/
*.swp
Thumbs.db
Desktop.ini
ehthumbs.db
$RECYCLE.BIN/
.DS_Store
